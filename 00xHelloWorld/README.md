# 00xHelloWorld
---

This directory hosts the first script written on this journey of mine.

As is customary, I have created a Hello World script to mark the beginning
of my interaction with a programming language.

The script has a *shebang*

It prints the string *Hello World* to the console.

To run the script (assuming at that point you would be having the file locally),
open the directory of the file in the console and then run the command ./hello.py

Just like that, voila! :wink: