# Variables and Data Types

As you might know, everything in Python is an object. This repo
holds a few modules demonstrating how we can create objects
of different data types, and how what we can do with the values
of the objects with defferent types.

## List of modules

- ave.py  
Stores student grades in a dictionary and calculate the average.

- conv.py  
Convert a list of strings into integers and sum them.

- dup.py  
Use a set to remove duplicates from a list of numbers. 

- vdt.py  
Creates variables for name, age, and height, then print them in a formatted sentence.
