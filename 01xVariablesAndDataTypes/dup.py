my_list = [1, 2, 3, 4, 5, 5, 4, 3, 2, 1, 11]

no_dup = set(my_list)
print(no_dup)

"""
This script uses a set to remove duplicates from a list of numbers

- Defines a list with duplicates
- Uses the set() function to convert the list into a set, automatically
  removing duplicates.

Finally we print the result with no duplicates
"""